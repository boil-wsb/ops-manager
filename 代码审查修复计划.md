# 代码审查修复计划

> 范围：基于 2026-07-17 对抗式代码审查报告，**排除安全类问题**（认证绕过、越权、SSRF、PII 泄露、限流、IP 伪造、生产 IP 泄露等），仅覆盖功能正确性、数据完整性、并发安全、可靠性问题。
>
> 总计需修复：Critical 13 项（C-01 ~ C-13）+ Important 23 项（I-01 ~ I-24，其中 I-10 与 C-06 同项不重复计数）+ Minor（可选）。

---

## 已修复项（2026-07-17 会话，不在本计划执行范围内）

以下 3 项在 2026-07-17 的会话中已修复并验证通过，本计划执行时跳过，仅作记录避免重复劳动：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| P0-E | POST `/notify` 幂等性缺失 + PATCH 端点破坏 `open_message_id` + 1:N 批量更新缺失 | `feishu_notifications.py` | ✅ 已验证（2026-07-17 11:08） |
| P1-C | `NotificationRecord` 时钟不一致（created_at 用 Python 时钟，updated_at 用 DB 时钟）+ CRUD onupdate 不触发 | `notification_record.py`、`notification_callback_log.py`、`crud_notification_record.py` | ✅ 已验证（2026-07-17 11:08） |

**注意**：P0-E 和 P1-C 是 devops-webhook 通知路径的修复，与本计划 C-03（`alerts.py:469` 的 webhook 保存路径 `contextlib.suppress`）是**两个独立路径**，C-03 仍需修复。

---

## 第二批已修复项（2026-07-17 会话，4 项低风险日志/查询修复）

以下 4 项为第二批「日志脱敏 + 小改动」修复，已通过 webhook 测试验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| I-04 | 完整 webhook payload 未脱敏直接记录 | `alerts.py`（import `sanitize_sensitive_data` + 截断到 500 字符） | ✅ 已验证（2026-07-17 11:49，`password`/`api_key` 已脱敏，total=527 截断到 500） |
| I-05 | 飞书卡片 JSON 完整内容未截断 | `notification_task.py:228`（`json.dumps` + 截断到 500 字符 + fallback） | ✅ 已验证（2026-07-17 11:49，total=1175 截断到 500） |
| C-02 | `_update_alert_history_notification_sent` 缺 `ORDER BY` + `starts_at` 过滤 | `notification_task.py:559-626`（新增 `starts_at` 参数 + `AND starts_at = :starts_at` + `ORDER BY id DESC LIMIT 1`，调用方传入） | ✅ 已验证（2026-07-17 11:49，命中 id=3842 正确记录） |
| C-05 | `_save_firing_alert_card_messages` 查询 `status='firing'` 在时序交错时返回空 | `notification_task.py`（`NotificationContext` 加 `history_id` 字段 + `_save_firing_alert_card_messages` 优先用主键精确查询） | ✅ 已验证（2026-07-17 11:49，使用 ctx.history_id=3842 保存 2 条卡片） |

**第二批剩余 5 项**（C-01、C-03、I-01、I-02、I-11）待后续执行：
- I-11 已验证无需修改（迁移脚本正确使用 `text() + get_bind().execute()`，DDL 带括号）
- C-01/C-03/I-01/I-02 涉及流程改动，建议下一批一起做

## 第二批剩余项已修复（C-01/C-03/I-01/I-02，2026-07-17 会话）

以下 4 项为第二批剩余项的修复，已通过闭环测试验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| C-01 | firing 全部收件人失败时预占位 `notification_sent=True` 不回滚 → Alertmanager 重发去重通过 → 告警永久丢失 | `notification_task.py`（`save_notification_result` 入口加 firing 全部失败回滚逻辑，使用 `ctx.history_id` + 独立 DB session，符合三阶段分离架构） | ✅ 已验证（2026-07-17，回滚 notification_sent=False） |
| C-03 | `_save_op` 被 `contextlib.suppress` 静默吞掉，保存 message_id 失败无日志 | `alerts.py`（移除 `contextlib.suppress`，改为 `try/except` + `result["save_failed"]=True` 标志 + ERROR 日志含 traceback） | ✅ 已验证（2026-07-17，代码审查确认） |
| I-01 | 批量 resolve 误伤 SUPPRESSED 与跨周期记录 | `alerts.py`（批量 resolve 查询加 `starts_at` 精确过滤 + `status='firing'` 显式过滤，排除 SUPPRESSED 和 RESOLVED；`starts_at` 为 None 时跳过批量 resolve） | ✅ 已验证（2026-07-17，3 条测试记录：上一周期 firing 不受影响、当前周期 firing 改 resolved、当前周期 suppressed 不受影响） |
| I-02 | resolved 部分卡片更新失败时 `alert_history.status` 仍改 resolved | `notification_task.py`（`save_notification_result` resolved 分支加 `total_cards vs updated_cards` 判断，部分失败时只 commit card_status 更新，保持 `alert_history.status=firing`，记录失败卡片 ID 供重试） | ✅ 已验证（2026-07-17，1/2 成功保持 firing + 2/2 成功改 resolved） |

**实施细节**：
- C-01 修复位置：阶段 3 `save_notification_result` 内（**不能在阶段 2 `execute_feishu_notification` 内**，否则违反三阶段分离架构核心不变式——阶段 2 不持有 DB session）
- C-01 回滚失败时由 `db_operation_with_retry` 的 try/except 兜底，记录 ERROR 日志但不抛异常
- C-03 `result["save_failed"]=True` 供调用方感知保存失败，主流程继续返回 result 不抛异常
- I-01 原 SQL `status != RESOLVED` 会命中 SUPPRESSED，改为 `status = 'firing'` 显式过滤
- I-01 `starts_at` 为 None 时跳过批量 resolve（向后兼容，理论上 Alertmanager 总会带 startsAt）
- I-02 部分失败时 `failed_card_message_ids` 写入日志，供下次 resolved webhook 重试参考
- I-02 全部成功时 `updated_cards == total_cards` 才改 `alert_history.status=resolved`

**第二批全部完成**：11 项（I-04/I-05/C-02/C-05/C-04/C-06/I-11/C-01/C-03/I-01/I-02）✅

## 第二批附加修复（C-04/C-06 并发安全与约束，2026-07-17 会话）

以下 2 项为 C-04/C-06 并发安全修复，采用方案 C（A+B 组合）+ 按计划实施，已通过端到端 DB 测试验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| C-04 | TOCTOU 竞态：去重检查无行锁 | `alerts.py`（`existing_query.with_for_update()`）+ `crud_alert.py`（`create_from_alertmanager` 捕获 `IntegrityError` 重试 SELECT）+ 迁移 `20260717_0003_add_alert_history_firing_unique.py`（部分唯一索引 `WHERE status='firing'`） | ✅ 已验证（2026-07-17 12:04，`UniqueViolationError` 拒绝并发 INSERT；不同 starts_at 与 resolved 状态 INSERT 不受影响） |
| C-06 | `AlertCardMessage` 缺 `(alert_history_id, recipient_open_id)` 唯一约束 | `alert.py`（`UniqueConstraint`）+ 迁移 `20260717_0002_add_alert_card_msg_unique.py` + `notification_task.py`（INSERT 改用 `ON CONFLICT DO UPDATE`） | ✅ 已验证（2026-07-17 12:04，重试场景 ON CONFLICT 更新 message_id 而非插入新记录，表内仅 1 条记录） |

**实施细节**：
- C-04 部分唯一索引 DDL：`CREATE UNIQUE INDEX ix_alert_history_firing_unique ON alert_history (alertname, (labels ->> 'instance'), starts_at) WHERE status = 'firing'`
- C-04 应用层双保险：`with_for_update()` 锁定已存在行 + `IntegrityError` 捕获后重新 SELECT（走 `with_for_update`）
- C-06 当前表无重复数据（223 条记录，0 个重复组），可直接加约束无需清理
- C-06 ON CONFLICT 语句：`INSERT ... ON CONFLICT (alert_history_id, recipient_open_id) DO UPDATE SET open_message_id=EXCLUDED.open_message_id, card_status='firing'`

---

## 第三批修复（建议功能状态机 + 飞书回调，2026-07-17 会话）

以下 6 项为建议审批状态机、飞书回调线程、查询码生成、卡片发送重试相关修复，已通过端到端 DB 测试 + 代码审查验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| C-07 | 建议审批状态机完全无校验（回调路径 UPDATE 无 `WHERE status=...`） | `callback_handler.py`（approve/reject/archive 三处 UPDATE 加 `AND status='pending'`/`AND status='approved'` 前置条件 + `rowcount` 检查） | ✅ 已验证（2026-07-17，第一次 approve rowcount=1、第二次 rowcount=0 跳过；archive 同理） |
| C-08 | 并发审批产生重复市场部卡片 | `callback_handler.py`（依赖 C-07 的 `WHERE status='pending'` + `rowcount=0` 跳过卡片发送 + WARNING 日志） | ✅ 已验证（2026-07-17，rowcount=0 时不调用 `_sync_send_market_card`） |
| I-06 | `generate_query_code` TOCTOU + fallback 不查重 | `suggestion_service.py`（fallback 8-char 路径进入查重循环 + 耗尽抛 RuntimeError）+ `suggestions.py`（调用方捕获 `IntegrityError` 后重新生成 code 并重试 3 次） | ✅ 已验证（2026-07-17，生成 6-char code + fallback 查重循环存在 + RuntimeError 兜底） |
| I-07 | 飞书卡片发送失败静默吞掉，建议永久卡 pending | `suggestion_service.py`（新增 `_send_with_retry` 3 次指数退避重试 1s→2s→4s + 全部失败返回 None + 重试日志） | ✅ 已验证（2026-07-17，`max_retries=3` + 指数退避 `2 ** (attempt - 1)` + 重试日志） |
| I-09 | `asyncio.run()` 在线程内反复创建事件循环 | `callback_handler.py`（`_async_*` 重构为 `_sync_*`，直接调用飞书 service 同步 API）+ `suggestion_service.py`（新增 `*_sync` 同步版本 + async 版本改为薄包装 `asyncio.to_thread`） | ✅ 已验证（2026-07-17，5 个 `_sync_*` 函数存在 + AST 解析确认 `_handle_suggestion_*` 无 `asyncio.run()` 调用） |
| I-24 | 回调路径 `market_result` 无长度校验 | `callback_handler.py`（`_handle_suggestion_archive` 入口截断到 2000 字符 + 日志记录 `original_len`/`truncated_to`） | ✅ 已验证（2026-07-17，源码有 `market_result[:2000]` + 模拟截断 3000→2000） |

**实施细节**：
- C-07/C-08：approve 和 reject 的 assignment UPDATE 加 `AND status='pending'`；archive 的 suggestion UPDATE 加 `AND status='approved'`。`rowcount=0` 时跳过后续卡片操作（更新原卡片/发送市场部卡片），记录 WARNING 日志含 `current_status` 和 `skipped=True`
- I-06：`generate_query_code` 的 SELECT 查重存在 TOCTOU 竞态，由 DB 层 `query_code` unique 约束兜底；调用方 `suggestions.py` 捕获 `IntegrityError` 后 rollback + 重新生成 code，最多重试 3 次
- I-07：`_send_with_retry` 封装飞书 `send_message_to_user` 调用，3 次重试（1s→2s→4s），重试成功/失败均有结构化日志；全部失败返回 None，调用方通过 None 区分"未发送"和"发送失败"
- I-09：`suggestion_service.py` 新增 5 个同步版本函数（`send_pending_card_sync`/`send_market_card_sync`/`update_to_approved_sync`/`update_to_rejected_sync`/`update_to_archived_sync`），async 版本改为 `asyncio.to_thread(sync_version, ...)` 薄包装；`callback_handler.py` 的 `_sync_*` 函数委托给同步版本，移除所有 `asyncio.run()` 调用
- I-24：`market_result` 截断在 `_handle_suggestion_archive` 函数入口（DB 操作之前），避免超长文本写入 DB 和渲染卡片异常

**第三批全部完成**：6 项（C-07/C-08/I-06/I-07/I-09/I-24）✅

---

## 第四批修复（TerminalMetric 唯一约束 + Asset CRUD 原子性，2026-07-17 会话）

以下 5 项为终端指标唯一约束、批量 upsert 事务一致性、资产 CRUD 重名检测/字段清空/原子提交相关修复，已通过 DB 约束验证 + 代码审查验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| C-09 | `TerminalMetric.asset_id` 无 UNIQUE 约束，`limit(1)` 防不住并发写入 | `terminal_metric.py`（`UniqueConstraint("asset_id")`）+ 迁移 `20260717_0004`（清理重复 + 创建约束）+ `crud_terminal_metric.py`（`upsert_metric` 改用 `pg_insert.on_conflict_do_update`） | ✅ 已验证（2026-07-17，约束存在 + 0 重复组 + ON CONFLICT 代码确认） |
| C-10 | `bulk_upsert_metrics` 单次 commit + 计数器与事务不一致 | `crud_terminal_metric.py`（每项 `begin_nested` savepoint + `failed_items` 列表 + 返回 `{created, failed}` dict） | ✅ 已验证（2026-07-17，savepoint + dict 返回 + failed_items 代码确认） |
| I-12 | `crud_asset.py` `limit(1)` 静默丢弃重名用户 | `crud_asset.py`（新增 `_resolve_owner_id_by_name` helper，`limit(2)` 检测重名 + WARNING 日志） | ✅ 已验证（2026-07-17，limit(2) + logger.warning 代码确认） |
| I-13 | `update_with_labels` 跳过 None，无法清空字段 | `crud_asset.py`（移除 `if value is not None` 守卫 + `owner_name=None` 时同步清空 `owner_id`） | ✅ 已验证（2026-07-17，守卫移除 + owner_name=None 清空逻辑确认） |
| I-14 | `create_with_labels` / `update_with_labels` 双 commit 非原子 | `crud_asset.py`（合并资产和审计历史为单次 commit） | ✅ 已验证（2026-07-17，create/update 各 1 次 commit 代码确认） |

**实施细节**：
- C-09：迁移 `20260717_0004_add_terminal_metric_asset_id_unique.py`（`down_revision = "add_history_firing_unique"`），upgrade 先 DELETE 重复数据（保留 `MAX(id)`）再创建 `uq_terminal_metric_asset_id` UNIQUE 约束；`upsert_metric` 改用 `pg_insert(TerminalMetric).values(**data).on_conflict_do_update(index_elements=["asset_id"], set_=update_set)`，原子性保证，彻底消除 TOCTOU 竞态
- C-10：`bulk_upsert_metrics` 每项用 `async with db.begin_nested()` savepoint 包裹，单项失败记录到 `failed_items` 列表并继续循环，返回 `{"created": N, "failed": [...]}`；计数器仅在 savepoint 成功后累加，与实际写入一致
- I-12：新增 `_resolve_owner_id_by_name(db, owner_name)` helper，`limit(2)` 取最多 2 条，`len > 1` 时记 WARNING 日志含 `matched_ids`，返回 `(owner_id, has_duplicates)` 元组；`create_with_labels` 和 `update_with_labels` 均改用此 helper
- I-13：移除 `for field, value in update_data.items(): if value is not None:` 守卫；`exclude_unset=True` 已确保只有显式提供的字段在 `update_data` 中，None 值表示用户想清空该字段（如 `description`/`mac_address`/`private_ip`）；`owner_name=None` 时同步设置 `owner_id=None`
- I-14：`create_with_labels` 和 `update_with_labels` 将 `AssetHistory` 在 `db.commit()` 前添加，合并为单次 commit，资产和审计历史原子写入

**第四批全部完成**：5 项（C-09/C-10/I-12/I-13/I-14）✅

---

## 第五批修复（迁移去重 + Prometheus 客户端健壮性 + 证书分页，2026-07-17 会话）

以下 6 项为迁移脚本去重、asyncio 异常回调、Prometheus 客户端异常处理/IP 校验、证书列表服务端分页相关修复，已通过代码审查 + 功能测试 + 前端 build 验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| I-08 | 迁移 `ON CONFLICT DO NOTHING` 对 `notification_groups` 无效（无 UNIQUE 约束，不触发去重） | `20260714_0002_add_suggestion_permissions.py`（notification_groups INSERT 改用 `WHERE NOT EXISTS` 子查询按 notification_type 查重；permissions 的 `ON CONFLICT (code)` 保留因 code 有 UNIQUE 约束） | ✅ 已验证（2026-07-17，WHERE NOT EXISTS + notification_type 过滤代码确认） |
| I-16 | `asyncio.create_task` 无异常回调，任务异常被静默吞掉 | `assets.py`（新增 `_log_task_exception` 回调 + `add_done_callback`，cancelled 检查 + `logger.exception` 含 traceback） | ✅ 已验证（2026-07-17，回调函数 + cancelled 检查代码确认） |
| I-17 | `get_targets` 静默吞异常返回 `[]`，调用方误判为"无 targets" | `prometheus/client.py`（移除 try/except + `return []`，保留 `raise_for_status` 让 HTTP 错误抛出；两处调用方 `get_all_windows_nodes`/`get_all_windows_nodes_health_check` 加 try/except 降级为 WARNING 日志 + 空 addr_map） | ✅ 已验证（2026-07-17，get_targets 无 catch+return[] + 两处调用方降级 WARNING 日志代码确认） |
| I-18 | `_extract_ip` 不校验 IP 格式，主机名/异常输入被当作 IP 返回 | `prometheus/client.py`（新增 `ipaddress.ip_address(cleaned)` 校验，非法返回 `""` + WARNING 日志含 address/extracted） | ✅ 已验证（2026-07-17，6 个功能测试用例：IPv4:port/http://IPv4/纯IPv4/主机名拒绝/localhost拒绝/空字符串 全部通过） |
| I-19 | 证书列表移除分页全量返回，证书数量增长时单次响应过大 | 后端 `ops.py` + `schemas/ops.py`（新增 `page`/`page_size` 参数上限 500 + `CertificateListResponse` 含 total/items/page/pageSize/totalPages）；前端 `ops.ts` + `CertificateList.tsx`（queryKey 加分页参数 + `keepPreviousData` 翻页不闪烁 + `data?.items`/`data?.total` 读取） | ✅ 已验证（2026-07-17，后端 schema + 前端 TypeScript build 通过 + keepPreviousData + queryKey 代码确认） |
| I-20 | 迁移未过滤空字符串 `feishu_open_message_id=''` | `20260717_0001_create_alert_card_messages.py`（创建时已包含 `WHERE feishu_open_message_id IS NOT NULL AND feishu_open_message_id != ''` 过滤） | ✅ 已验证（2026-07-17，过滤条件代码确认） |

**实施细节**：
- I-08：`notification_groups` 表无 UNIQUE 约束，`ON CONFLICT DO NOTHING` 无目标约束时不触发去重，迁移回滚重跑会产生重复行。改用 `INSERT ... SELECT ... WHERE NOT EXISTS (SELECT 1 FROM notification_groups WHERE notification_type = 'suggestion_market_review')`。**注意**：permissions 表的 `ON CONFLICT (code) DO NOTHING` 正确保留，因 permissions.code 有 UNIQUE 约束
- I-16：`asyncio.create_task` 返回的 Task 对象若未添加 done_callback，任务内异常只在 GC 回收时打印警告，调用方误以为已触发。回调内先检查 `t.cancelled()`，再取 `t.exception()`，非 None 时 `logger.exception` 记录 ERROR 日志
- I-17：`get_targets` 原 `catch Exception + return []` 是"静默失败"反模式，调用方无法区分"无 targets"和"查询失败"。改为抛出异常后，两处调用方（Windows 节点 IP 解析）按需 try/except 降级为 WARNING 日志 + 空 addr_map，不影响节点发现本身
- I-18：原 `_extract_ip` 直接字符串拆分，"windows-host-01:9100" 会被解析为 "windows-host-01" 当作 IP 入库。用 `ipaddress.ip_address()` 严格校验，非法返回 `""` + WARNING 日志。**注意**：IPv6 地址（如 `[::1]:9100`）当前拆分逻辑可能不准确，但项目当前仅使用 IPv4，后续如需 IPv6 支持再扩展
- I-19：原接口返回 `list[CertificateResponse]` 全量数据，前端分页。改为服务端分页返回 `CertificateListResponse {total, items, page, pageSize, totalPages}`，上限 500/页。前端用 React Query `keepPreviousData`（v5 中为 `placeholderData: keepPreviousData`）翻页时保留上一页数据避免闪烁，queryKey 包含 page/pageSize 触发重新请求
- I-20：迁移脚本创建时已包含空字符串过滤，本批次仅代码审查确认，无需修改

**第五批全部完成**：6 项（I-08/I-16/I-17/I-18/I-19/I-20）✅

---

## 第二轮审查修复（P0+P1 共 10 项，2026-07-17 会话）

以下 10 项为对抗式第二轮审查发现的新缺陷修复（3 个 P0 + 7 个 P1），已通过代码审查 22/22 PASS + NC-1 闭环测试（资产创建 HTTP 201，ID=437 正确生成）+ ND-4 验证（owner_name 解析失败时 owner_name=None, owner_id=None 数据一致）：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| NC-1 | I-14 `create_with_labels` 合并 commit 后 `db_obj.id` 在 flush 前为 None，`AssetHistory.asset_id` NOT NULL 约束违反，资产创建接口整体不可用 | `crud_asset.py`（`db.add(db_obj)` 后加 `await db.flush()` 让 id 先生成，再构造 AssetHistory） | ✅ 已验证（2026-07-17，闭环测试 HTTP 201 + 资产 ID=437 非 None） |
| NC-2 | I-06 fallback 8-char code 与 DB 列 VARCHAR(6) 不兼容，触发 `DataError`（非 IntegrityError 子类，调用方无法捕获，直接 500） | `suggestion_service.py`（fallback 改为 6 字符 + 循环次数 20→30→50，不依赖放宽长度） | ✅ 已验证（2026-07-17，无 range(8) + 30+50=80 次循环代码确认） |
| NC-3 | C-02 遗漏分支：「无飞书模板」路径调用 `_update_alert_history_notification_sent` 未传 `starts_at`，走退化路径可能误标记其他周期告警 | `notification_task.py`（无模板分支补传 `starts_at=starts_at`） | ✅ 已验证（2026-07-17，starts_at=starts_at 代码确认） |
| ND-1 | C-04 IntegrityError 重试 SELECT 未过滤 `status='firing'`，会命中已 resolved 记录并加锁 | `crud_alert.py`（重试 SELECT 加 `AlertHistory.status == "firing"` 过滤） | ✅ 已验证（2026-07-17，status='firing' 过滤代码确认） |
| ND-2 | I-01 `starts_at=None` 时跳过批量 resolve，旧 firing 记录保持 firing | `alerts.py`（`starts_at is None` 时 fallback `now_shanghai()`，不再跳过） | ✅ 已验证（2026-07-17，effective_starts_at fallback 代码确认） |
| ND-3 | I-07 `update_card_message` 路径无重试且吞异常，审批后更新原卡片失败时卡片永久显示错误状态 | `suggestion_service.py`（新增 `_update_card_with_retry` 3 次指数退避重试 + 检查返回值 success 字段；3 个 `update_to_*_sync` 函数全部改用此函数） | ✅ 已验证（2026-07-17，3 处调用 + 无直接 update_card_message 代码确认） |
| ND-4 | I-13 `owner_name` 解析失败时 `owner_id` 不更新但 `owner_name` 仍写入，数据不一致 | `crud_asset.py`（create 解析失败时清空 `obj_in.owner_name=None`；update 解析失败时清空 `update_data["owner_id"]=None`） | ✅ 已验证（2026-07-17，闭环测试 owner_name=None, owner_id=None） |
| ND-5 | I-12 `has_duplicates` 被调用方 `_` 丢弃，重名检测形同虚设 | `crud_asset.py`（create/update 均接收 `has_duplicates` 变量，不再用 `_` 丢弃） | ✅ 已验证（2026-07-17，has_duplicates 变量接收代码确认） |
| ND-6 | I-24 API 路径 `archive_suggestion` 写入 `market_result` 无截断，与回调路径 2000 字符限制不一致 | `suggestions.py`（API 路径截断到 2000 字符 + 日志记录 original_len/truncated_to） | ✅ 已验证（2026-07-17，market_result[:2000] + original_len 代码确认） |
| ND-7 | I-03 `_try_forward_callback` 异常路径不 dispose 原 engine，连接池泄漏 | `callback_handler.py`（异常路径开头加 `try: engine.dispose() except: pass`） | ✅ 已验证（2026-07-17，异常路径 dispose 代码确认） |

**实施细节**：
- NC-1：`db.add(db_obj)` 后 `await db.flush()` 让 SQLAlchemy INSERT 资产行并获取自增 id，再构造 `AssetHistory(asset_id=db_obj.id, ...)`。**关键教训**：`db.add()` 不会立即生成 id，需要 `flush()` 才会 INSERT；合并 commit 时必须先 flush 让父表 id 生成，再构造依赖该 id 的子表记录
- NC-2：`query_code` 列为 `VARCHAR(6)`，原 fallback 生成 8 字符会触发 `DataError`（非 `IntegrityError` 子类）。改为 fallback 仍用 6 字符 + 增加循环次数（30+50=80），理论容量 32^6≈10 亿，80 次循环耗尽概率接近 0
- NC-3：「无飞书模板」分支调用 `_update_alert_history_notification_sent` 未传 `starts_at`，走 else 退化路径（无 starts_at 过滤、无 status 过滤），可能误标记其他周期告警。补传 `starts_at=starts_at`
- ND-1：IntegrityError 由部分唯一索引 `WHERE status='firing'` 触发，重试 SELECT 也应只查 firing，避免命中已 resolved 记录并加锁（会把已 resolved 记录改回 firing，与 I-01 修复意图冲突）
- ND-2：`starts_at is None` 时 fallback `now_shanghai()`，不再跳过批量 resolve。Alertmanager 协议保证总会带 startsAt，但防御性处理避免漏处理
- ND-3：`_update_card_with_retry` 与 `_send_with_retry` 对称，包装 `update_card_message` 调用。`update_card_message` 内部 catch 异常返回 `{"success": False}` 不抛异常，此函数主动检查返回值并重试
- ND-4：owner_name 解析失败时（用户名不存在），create 清空 `obj_in.owner_name=None`，update 清空 `update_data["owner_id"]=None`，避免 owner_name 与 owner_id 不一致
- ND-5：create/update 均接收 `has_duplicates` 变量（不再用 `_` 丢弃），重名时 `_resolve_owner_id_by_name` 内部已记 WARNING 日志
- ND-6：API 路径 `archive_suggestion` 截断 `market_result` 到 2000 字符，与回调路径 `_handle_suggestion_archive` 行为一致
- ND-7：`_try_forward_callback` 异常路径开头加 `try: engine.dispose() except: pass`，检查 `"engine" in dir()` 防止变量未定义。**注意**：全项目还有 52 处直接 `create_engine()` 调用，本次仅修复审查者明确指出的 `_try_forward_callback` 异常路径，其他地方后续统一改造为 contextmanager 模式

**第二轮审查 P0+P1 全部完成**：10 项（NC-1/NC-2/NC-3 + ND-1~ND-7）✅

---

## 第二轮审查 P2 修复（共 5 项，2026-07-17 会话）

以下 5 项为第二轮审查 P2（Minor / 可维护性）修复，已通过代码审查 + 48 个测试用例通过 + 模块导入完整性验证：

| 编号 | 问题 | 修复文件 | 验证状态 |
|------|------|---------|---------|
| P2-1 | 核心修复缺单测覆盖，违反工作区规则"功能改造的唯一判断标准是闭环测试" | 新增 3 个测试文件 + 补强 1 个既有测试 | ✅ 已验证（2026-07-17，48 passed + 3 skipped） |
| P2-2 | 诊断脚本 `backend/scripts/check_card_messages.py` 残留（一次性 bug 调查脚本） | `scripts/check_card_messages.py`（直接删除） | ✅ 已验证（2026-07-17，文件已删除） |
| P2-3 | `callback_handler.py` 3 处 `lark.JSON.marshal(data)` 完整序列化 data 含敏感字段，违反日志脱敏规则 | `callback_handler.py`（3 处改为字段级日志 + open_id 截断到前 8 字符） | ✅ 已验证（2026-07-17，3 处字段级日志 + open_id[:8] 截断代码确认） |
| P2-4 | `alerts.py:5` 死导入 `contextlib`（C-03 修复后残留，仅在注释中提及） | `alerts.py`（删除 `import contextlib`） | ✅ 已验证（2026-07-17，导入完整性测试通过） |
| P2-5 | `crud_terminal_metric.py` `bulk_upsert_metrics` 死代码（无任何调用方，C-10 修复对象但保留无用代码违反 YAGNI） | `crud_terminal_metric.py`（删除该方法，alembic 注释保留为历史记录） | ✅ 已验证（2026-07-17，全项目无 bulk_upsert_metrics 调用方） |

**实施细节**：

- **P2-1 测试覆盖**：
  - 新增 `tests/services/test_asset_create_flush.py`：NC-1 回归测试，验证 `db.flush()` 在 `AssetHistory` 构造前被调用 + 调用顺序 `add(Asset) → flush → add(AssetHistory) → commit` + `db_obj.id` 在 flush 后非 None + `AssetHistory.asset_id` 等于已 flush 的 `db_obj.id`
  - 新增 `tests/services/test_crud_alert_integrity_recovery.py`：ND-1 回归测试（3 个用例），验证 IntegrityError 重试 SELECT 返回 firing 记录（非 resolved）+ 重试 SELECT 的 SQL 包含 `status='firing'` 过滤条件 + 无 instance 时不走重试 SELECT
  - 新增 `tests/services/test_prepare_alert_no_template.py`：NC-3 回归测试（2 个用例），验证无 feishu_template 时 `_update_alert_history_notification_sent` 收到 `starts_at` 非 None 且为 datetime 对象 + resolved 状态时不调用 mark_sent
  - 补强 `tests/api/test_assets.py::test_create_asset`：NC-1 集成断言，201 响应时 `asset["id"]` 不为 None（防止 flush 缺失导致 500 回归）
  - 补强 `tests/services/test_process_alert_unit.py`：新增 `TestProcessAlertResolvedStartsAtNone` 测试类，ND-2 回归测试，验证 `startsAt=None` 时 resolved 仍走批量更新（fallback `now_shanghai()`）

- **P2-3 日志脱敏**：3 处 `_do_bot_p2p_chat_entered` / `_do_im_message_reaction_created_v1` / `_do_im_message_message_read_v1` 不再 `lark.JSON.marshal(data)` 完整序列化（含敏感字段），改为字段级日志 + `open_id[:8] + "..."` 截断。同时移除冗余的 `lark = _get_lark_module()` 调用（这些函数已不需要 lark 模块）

- **P2-5 死代码评估结论**：`bulk_upsert_metrics` 在 `backend/app` 中除定义处无任何调用方（仅 alembic 迁移注释提及），是真正的死代码。单条 `upsert_metric` 已能覆盖现有需求。按 YAGNI 原则删除。C-10 修复成果通过 alembic 注释和 git 历史保留

**遗留问题**（不阻塞合并，列入后续工作）：
- `tests/services/test_alert_failure_paths.py` / `test_db_session_lifecycle.py` / `test_notification_task_unit.py` 3 个既有测试文件因 `send_alert_notification` 在三阶段分离重构后被删除而无法收集（与本轮 P2 工作无关，是重构遗留 bug）。后续需要把这 3 个文件迁移到新的 `prepare_alert_notification` / `execute_feishu_notification` / `save_notification_result` 三阶段 API

**第二轮审查 P2 全部完成**：5 项（P2-1~P2-5）✅

---

## 优先级分级

- **P0（阻断合并）**：Critical 全部
- **P1（合并前修复）**：Important 中影响数据一致性/可靠性项
- **P2（后续迭代）**：Important 中影响性能/可维护性项 + Minor

---

## P0 — Critical 修复清单

### C-01 告警预占位 `notification_sent=True` 在全部发送失败时不回滚

> ✅ **已修复**（见顶部「第二批剩余项已修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/alerts/notification_task.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/alerts/notification_task.py)（约 319-395 行 `prepare_alert_notification` + 约 458-500 行 `save_notification_result`）
- **问题**：firing 路径在 `prepare_alert_notification` 阶段预占位 `notification_sent=True`，若 `execute_feishu_notification` 中 `sent_cards` 为空（所有收件人失败），`needs_save=False`，预占位永久 True → Alertmanager 重发时去重通过 → 告警永久丢失。
- **修复步骤**：
  1. **修复位置：阶段 3 `save_notification_result` 内**（不能在阶段 2 `execute_feishu_notification` 内，否则违反三阶段分离架构核心不变式——阶段 2 不持有 DB session，飞书调用期间 DB 连接池必须空闲。详见 `alert-processing-flow-and-review.md` 违反 4 / 项目记忆 QueuePool 耗尽根因）。
  2. 在 `save_notification_result` 入口处，若 `ctx.is_firing and not result.sent_cards`，执行回滚：
     ```python
     if ctx.is_firing and not result.sent_cards:
         await db.execute(
             text("UPDATE alert_history SET notification_sent=false WHERE id=:id"),
             {"id": ctx.history_id},
         )
         await db.commit()
         logger.error(
             f"All recipients failed, rolled back notification_sent: "
             f"history_id={ctx.history_id}, alertname={ctx.alertname}, instance={ctx.instance}",
             extra={"action": "alert.notify", "history_id": ctx.history_id},
         )
         return
     ```
  3. 回滚失败时（DB 异常）由 `db_operation_with_retry` 的 try/except 兜底，记录 ERROR 日志（含 history_id、alertname、instance），不抛异常（避免影响主流程返回）。
  4. `ctx.history_id` 在 C-05 修复中已加入 `NotificationContext`，可直接使用。
- **影响范围**：告警通知主流程
- **验证方法**：
  - 单测：mock `send_p2p_card_message` 全部抛 SSL 错误，断言 `alert_history.notification_sent` 最终为 False
  - 已有测试 `test_alert_failure_paths.py` 可扩展
- **回归风险**：低。仅在全部失败时触发回滚，正常路径不变。

---

### C-02 `_update_alert_history_notification_sent` 缺 `ORDER BY` + `starts_at` 过滤

> ✅ **已修复**（见顶部「第二批已修复项」小节，2026-07-17 11:49 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/alerts/notification_task.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/alerts/notification_task.py)（约 567-580 行）
- **问题**：同 `alertname+instance` 多条 firing 记录时 `fetchone()` 返回任意行，可能标记到旧记录，当前记录去重失效。
- **修复步骤**：
  1. SQL 增加 `AND starts_at = :starts_at` 过滤
  2. 增加 `ORDER BY id DESC LIMIT 1`
  3. 函数签名增加 `starts_at: datetime` 参数
  4. 调用方 `prepare_alert_notification` 传入 `alert_data.starts_at`
- **影响范围**：告警去重
- **验证方法**：单测构造同一 alertname+instance 两条不同 starts_at 的 firing 记录，断言只标记当前一条
- **回归风险**：低

---

### C-03 `_save_op` 被 `contextlib.suppress` 静默吞掉

> ✅ **已修复**（见顶部「第二批剩余项已修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/api/v1/alerts.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/alerts.py)（约 469-470 行）
- **问题**：保存 message_id 失败时无日志无告警，resolved webhook 查不到 firing 卡片 → 卡片永久停留 firing。
- **修复步骤**：
  1. 移除 `with contextlib.suppress(Exception):`
  2. 改为 `try: await db_operation_with_retry(_save_op, ...) except Exception as e: logger.exception(...)`
  3. 在 `result` 对象上设置 `save_failed=True` 标志（如 AlertNotificationResult 已有字段则复用，否则新增）
  4. 主流程继续返回 `result`，不抛异常
- **影响范围**：告警卡片状态持久化
- **验证方法**：单测 mock `db_operation_with_retry` 抛异常，断言日志包含 ERROR 且 `result.save_failed=True`
- **回归风险**：低

---

### C-04 TOCTOU 竞态：去重检查无行锁

> ✅ **已修复**（见顶部「第二批附加修复」小节，2026-07-17 12:04 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/api/v1/alerts.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/alerts.py)（约 217-231 行）
- **问题**：并发 webhook 同时通过「无现有记录」检查 → N 条 history → N 次飞书发送。
- **修复步骤**：
  1. 在 `existing_query` 末尾加 `.with_for_update()`（PostgreSQL 行锁）
  2. 由于 `alert_history` 无 UNIQUE 约束（兼容历史数据，不加），`with_for_update` 仅锁定已存在的行；若 SELECT 返回 None，仍存在窗口
  3. 终极方案：在 `create_from_alertmanager` 的 INSERT 上加 `ON CONFLICT DO NOTHING`（需先加 UNIQUE 约束）
     - 但项目约束「不加 UNIQUE 约束兼容历史数据」→ 折中：新增部分唯一索引 `CREATE UNIQUE INDEX ... WHERE status='firing'`（partial unique index），仅对 firing 状态唯一，不影响历史 resolved 数据
  4. 推荐先实施 `with_for_update`（P0），部分唯一索引作为 P1
- **影响范围**：告警去重并发安全
- **验证方法**：并发测试（pytest-asyncio + asyncio.gather 模拟 5 个相同 webhook）
- **回归风险**：中。`with_for_update` 在事务隔离级别 REPEATABLE READ 下行为需验证；部分唯一索引需迁移

---

### C-05 firing 与 resolved 竞态产生孤儿卡片

> ✅ **已修复**（见顶部「第二批已修复项」小节，2026-07-17 11:49 验证通过）。以下为原始方案保留备查。

- **文件**：
  - [backend/app/services/alerts/notification_task.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/alerts/notification_task.py)（约 512-519 行）
  - [backend/app/api/v1/alerts.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/alerts.py)（约 144-160 行）
- **问题**：resolved 批量改 `status=resolved` 后，`_save_firing_alert_card_messages` 查询 `WHERE status='firing'` 返回空，已发卡片未保存 → resolved 永远找不到卡片。
- **修复步骤**：
  1. `prepare_alert_notification` 阶段已查询到 `history_id`，将其存入 `ctx.history_id`
  2. `_save_firing_alert_card_messages` 改为 `WHERE alert_history_id = :history_id`（精确主键查询），不再依赖 `status='firing'`
  3. 或者：prepare 阶段先 INSERT `alert_card_messages` 占位记录（status='firing'），execute 后再 UPDATE message_id
- **影响范围**：告警卡片生命周期
- **验证方法**：集成测试模拟 firing→resolved 时序交错
- **回归风险**：中

---

### C-06 `AlertCardMessage` 缺 `(alert_history_id, recipient_open_id)` 唯一约束

> ✅ **已修复**（见顶部「第二批附加修复」小节，2026-07-17 12:04 验证通过）。以下为原始方案保留备查。

- **文件**：
  - [backend/app/models/alert.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/models/alert.py)（约 110-127 行）
  - 新增 Alembic 迁移
- **问题**：重试/并发对同一收件人产生多条 firing 记录，resolved 只能更新一条。
- **修复步骤**：
  1. 模型 `AlertCardMessage.__table_args__` 增加 `UniqueConstraint("alert_history_id", "recipient_open_id", name="uq_alert_card_msg_recipient")`
  2. 新增迁移 `20260717_0002_add_alert_card_msg_unique.py`：
     - upgrade: 先清理重复数据（保留最新一条），再 `op.create_unique_constraint(...)`
     - downgrade: `op.drop_constraint(...)`
  3. `_save_firing_alert_card_messages` 的 INSERT 改用 `ON CONFLICT (alert_history_id, recipient_open_id) DO UPDATE SET open_message_id=EXCLUDED.open_message_id, card_status='firing'`
- **影响范围**：告警卡片数据完整性
- **验证方法**：迁移 upgrade/downgrade 各跑一次；并发测试
- **回归风险**：中。需先清理历史重复数据，迁移需谨慎

---

### C-07 建议审批状态机完全无校验（飞书回调路径）

> ✅ **已修复**（见顶部「第三批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/integrations/feishu/callback_handler.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py)（约 704-936 行）
- **问题**：回调路径直接 `UPDATE status='approved/rejected/archived'`，无 `WHERE status=...` 条件，可跨状态跳转。API 层有校验，回调绕过。
- **修复步骤**：
  1. `_handle_suggestion_approve`：UPDATE 语句加 `AND status = 'pending'`
  2. `_handle_suggestion_reject`：UPDATE 语句加 `AND status = 'pending'`
  3. `_handle_suggestion_archive`：UPDATE 语句加 `AND status = 'approved'`
  4. 每次执行后检查 `cursor.rowcount`，若为 0 说明状态已变更，记录 INFO 日志并 return（不发送后续卡片）
  5. 仅在 `rowcount=1` 时才调用 `_async_send_market_card` 等后续动作
- **影响范围**：建议状态机
- **验证方法**：单测覆盖：pending→approved（成功）、approved→approved（rowcount=0 不重复发卡片）、archived→approved（被拦截）、pending→archived（被拦截）
- **回归风险**：低

---

### C-08 并发审批产生重复市场部卡片

> ✅ **已修复**（见顶部「第三批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/integrations/feishu/callback_handler.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py)（约 704-715 行）
- **问题**：多人同时点"审批通过"，两个线程都 UPDATE 成功并发市场部卡片。
- **修复步骤**：依赖 C-07 的 `WHERE status='pending'` + `rowcount` 检查。第二个线程 rowcount=0，不发卡片。
- **影响范围**：建议通知
- **验证方法**：并发测试
- **回归风险**：低

---

### C-09 `TerminalMetric.asset_id` 无 UNIQUE 约束，`limit(1)` 防不住并发写入

> ✅ **已修复**（见顶部「第四批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：
  - [backend/app/crud/crud_terminal_metric.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_terminal_metric.py)（约 108-179 行）
  - [backend/app/models/terminal_metric.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/models/terminal_metric.py)
  - 新增 Alembic 迁移
- **问题**：并发 upsert 累积重复行，聚合查询把同一 asset 算多遍。
- **修复步骤**：
  1. 新增迁移 `20260717_0004_add_terminal_metric_asset_id_unique.py`（注意：编号 0003 已被 C-04 迁移占用，`down_revision = "add_history_firing_unique"`）：
     - upgrade: 清理重复数据（保留 id 最大的一条，删除其余）→ `op.create_unique_constraint("uq_terminal_metric_asset_id", "terminal_metric", ["asset_id"])`
     - downgrade: `op.drop_constraint(...)`
  2. 模型 `TerminalMetric.__table_args__` 增加 `UniqueConstraint("asset_id")`
  3. `upsert_metric` 改用 PostgreSQL `INSERT ... ON CONFLICT (asset_id) DO UPDATE`：
     ```python
     from sqlalchemy.dialects.postgresql import insert as pg_insert
     stmt = pg_insert(TerminalMetric).values(**data)
     stmt = stmt.on_conflict_do_update(index_elements=["asset_id"], set_={...})
     await db.execute(stmt)
     ```
  4. `bulk_upsert_metrics` 同样改用 ON CONFLICT
- **影响范围**：终端指标采集
- **验证方法**：迁移测试 + 并发 upsert 测试（10 个相同 asset_id 并发，断言表内仅 1 条）
- **回归风险**：中。数据清理需谨慎，建议先在测试环境演练

---

### C-10 `bulk_upsert_metrics` 单次 commit + 计数器与事务不一致

> ✅ **已修复**（见顶部「第四批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/crud/crud_terminal_metric.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_terminal_metric.py)（约 166-179 行）
- **问题**：第 N 项 flush 失败 → 整批回滚，但 `created_count` 已累加到 N-1，返回值错误。
- **修复步骤**：
  1. 用 `async with db.begin_nested()` (savepoint) 包裹单次 upsert
  2. 失败时记录错误项到 `failed_items` 列表，继续循环
  3. 循环结束后 commit 一次
  4. 返回值改为 `{created: N, failed: [...]}`
  5. 或者：失败即 raise，让调用方知道（更简单，符合 fail-fast）
- **影响范围**：批量同步
- **验证方法**：单测构造第 50 项 FK 不存在，断言返回值与实际写入一致
- **回归风险**：低

---

### C-11 commit 后 refresh relationship 不可靠

- **文件**：[backend/app/api/v1/departments.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/departments.py)（约 175-179 行）
- **问题**：清除负责人后接口可能仍返回旧 leader_username。
- **修复步骤**：
  1. 移除 `await db.refresh(dept, attribute_names=["leader"])`
  2. 改为重新查询：`result = await db.execute(select(Department).options(selectinload(Department.leader)).where(Department.id == dept_id))`，`dept = result.scalar_one()`
  3. 或更简单：`db.expire(dept)` 后再访问 `dept.leader`（触发重新加载）
- **影响范围**：部门负责人接口
- **验证方法**：单测覆盖：设置 leader → 清除 leader → 断言返回 leader_username=None
- **回归风险**：低

---

### C-12 健康检查 Windows CPU 阈值用错配置键

- **文件**：[backend/app/services/health_check_service.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/health_check_service.py)（约 170-171 行）
- **问题**：使用 `memory_usage_critical/warning` 而非 CPU 键。
- **修复步骤**：
  1. 新增配置项 `cpu_usage_percent_warning`（默认 80）、`cpu_usage_percent_critical`（默认 95）
  2. 在 [backend/app/config.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/config.py) 的 HealthCheckThresholds 或对应配置类中添加
  3. 代码改为 `cpu_critical = thresholds.get("cpu_usage_percent_critical", 95)`
  4. 更新 `.env.example` 添加新配置项
- **影响范围**：Windows 健康检查
- **验证方法**：单测覆盖不同阈值场景
- **回归风险**：低

---

### C-13 临时诊断文件仍暂存于 git 索引

- **文件**：`backend/_tmp_diag_hc.py`、`backend/_tmp_test_hc_api.py`、`.gitignore`
- **问题**：`git status` 显示 `AD`，工作区已删但索引仍持有 blob。`git commit` 会误提交。
- **修复步骤**：
  1. 执行 `git rm --cached backend/_tmp_diag_hc.py backend/_tmp_test_hc_api.py`
  2. 在 `.gitignore` 的 `# Misc` 段追加 `_tmp_*`
  3. 提交这两项变更
- **影响范围**：仓库卫生
- **验证方法**：`git status` 确认两文件不再出现；新建 `_tmp_test.py` 验证被忽略
- **回归风险**：无

---

## P1 — Important 修复清单（数据一致性/可靠性）

### I-01 批量 resolve 误伤 SUPPRESSED 与跨周期记录

> ✅ **已修复**（见顶部「第二批剩余项已修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/api/v1/alerts.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/alerts.py)（约 144-152 行）
- **修复**：批量 UPDATE 加 `AND starts_at = :starts_at` 过滤，仅影响当前周期；并排除 `SUPPRESSED` 状态
- **验证**：单测构造同 alertname+instance 不同 starts_at 的两条记录，断言只更新当前周期

### I-02 resolved 部分卡片更新失败时 `alert_history.status` 仍改 resolved

> ✅ **已修复**（见顶部「第二批剩余项已修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/alerts/notification_task.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/alerts/notification_task.py)（约 405-483 行）
- **修复**：仅当 `len(updated_card_message_ids) == len(ctx.resolved_card_messages)` 时才将 `alert_history.status` 改为 resolved；部分失败时保持 firing 并记录失败卡片列表供重试
- **验证**：单测 mock 1/3 卡片更新失败，断言 alert_history.status 保持 firing

### I-03 `_try_forward_callback` 每次新建 engine，异常路径不 dispose

- **文件**：[backend/app/integrations/feishu/callback_handler.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py)（约 47-322 行）
- **修复**：
  1. 使用模块级共享 engine（复用 `_get_sync_engine()`）
  2. 或用 `try/finally` 确保 `engine.dispose()`
- **验证**：异常路径测试，断言 engine 被释放

### I-04 完整 webhook payload 未脱敏直接记录

> ✅ **已修复**（见顶部「第二批已修复项」小节，2026-07-17 11:49 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/api/v1/alerts.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/alerts.py)（约 425 行）
- **修复**：对 payload 的 labels/annotations 应用敏感字段脱敏（password/token/api_key/authorization → ***），并截断到 500 字符
- **验证**：单测构造含 `password=xxx` 的 label，断言日志输出 `password=***`

### I-05 飞书卡片 JSON 完整内容未截断

> ✅ **已修复**（见顶部「第二批已修复项」小节，2026-07-17 11:49 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/alerts/notification_task.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/alerts/notification_task.py)（约 228 行）
- **修复**：`json.dumps(card, ensure_ascii=False)[:500]`
- **验证**：单测构造大卡片，断言日志长度 ≤ 500

### I-06 `generate_query_code` TOCTOU + fallback 不查重

> ✅ **已修复**（见顶部「第三批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/suggestion_service.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/suggestion_service.py)（约 28-38 行）
- **修复**：
  1. 捕获 `IntegrityError` 后重试生成
  2. fallback 路径同样进入查重循环
  3. 最多重试 5 次，仍冲突则 raise
- **验证**：并发测试

### I-07 飞书卡片发送失败静默吞掉，建议永久卡 pending

> ✅ **已修复**（见顶部「第三批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：
  - [backend/app/api/v1/suggestions.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/suggestions.py)（约 74-111 行）
  - [backend/app/services/suggestion_service.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/suggestion_service.py)（约 189-208 行）
- **修复**：
  1. `send_pending_card` 内实现 3 次指数退避重试（1s→2s→4s），与项目约束一致
  2. 全部失败后标记 suggestion `notification_status='failed'`（新增字段或用 status）
  3. 提供管理员「补发卡片」API
- **验证**：单测 mock 飞书 API 失败，断言重试 3 次且最终状态为 failed

### I-08 迁移 `ON CONFLICT DO NOTHING` 对 `notification_groups` 无效

> ✅ **已修复**（见顶部「第五批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/alembic/versions/20260714_0002_add_suggestion_permissions.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/alembic/versions/20260714_0002_add_suggestion_permissions.py)（约 75-86 行）
- **修复**：改用 `WHERE NOT EXISTS` 子查询：
  ```sql
  INSERT INTO notification_groups (...)
  SELECT ... WHERE NOT EXISTS (
      SELECT 1 FROM notification_groups WHERE notification_type = '...'
  )
  ```
- **验证**：迁移回滚后重跑，断言无重复

### I-09 `asyncio.run()` 在线程内反复创建事件循环，且飞书调用无重试

> ✅ **已修复**（见顶部「第三批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/integrations/feishu/callback_handler.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py)（约 729, 735, 872, 925, 939 行）
- **修复**：
  1. 将 `_handle_suggestion_*` 改为 async 函数
  2. 用 `asyncio.run_coroutine_threadsafe(coro, main_loop)` 提交到主事件循环
  3. 或在 service 层补充 3 次指数退避重试
- **验证**：性能测试对比回调耗时

### I-10 `AlertCardMessage` 缺约束（同 C-06，列入 P0）

### I-11 复合表达式索引 autogenerate 风险

> ✅ **已验证无需修改**（见顶部「第二批已修复项」小节，2026-07-17 验证通过：迁移脚本正确使用 `text() + get_bind().execute()`，DDL 带括号且无引号）。以下为原始方案保留备查。

- **文件**：
  - [backend/app/models/alert.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/models/alert.py)（约 80-85 行）
  - [backend/alembic/versions/20260716_0001_add_alert_history_dedup_index.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/alembic/versions/20260716_0001_add_alert_history_dedup_index.py)
- **修复**：核对迁移脚本使用 `op.get_bind().execute(text("CREATE INDEX ... (labels ->> 'instance') ..."))` 而非 `op.create_index`，确认 DDL 带括号且无引号
- **验证**：在测试库执行迁移，用 `\d alert_history` 检查索引定义

### I-12 `crud_asset.py` `limit(1)` 静默丢弃重名用户

> ✅ **已修复**（见顶部「第四批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/crud/crud_asset.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_asset.py)（约 107, 187 行）
- **修复**：
  1. 聚合查询 `SELECT id, COUNT(*) FROM users WHERE full_name=:name GROUP BY id` 检测重名
  2. 重名时记 WARNING 日志，要求调用方改用 `owner_id` 入参
  3. 或在 API 层返回 409 让用户明确选择
- **验证**：单测构造 2 个同名用户，断言日志含 WARNING

### I-13 `update_with_labels` 跳过 None，无法清空字段

> ✅ **已修复**（见顶部「第四批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/crud/crud_asset.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_asset.py)（约 191-196 行）
- **修复**：
  1. 区分「未提供」与「显式 null」
  2. 允许 null 清空的字段（description/mac_address/private_ip 等）直接赋值
  3. 或使用 `model_dump(exclude_unset=True)` 后对显式 None 也更新
- **验证**：单测覆盖清空字段场景

### I-14 `create_with_labels` / `update_with_labels` 双 commit 非原子

> ✅ **已修复**（见顶部「第四批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/crud/crud_asset.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_asset.py)（约 138-149, 205-214 行）
- **修复**：合并为单次 commit；或第二次 commit 失败时记 WARNING 但不回滚资产（明确接受审计可能丢失）
- **验证**：mock 第二次 commit 抛异常，断言行为符合预期

### I-15 `logging.py` uvicorn logger level 未显式设 INFO

- **文件**：[backend/app/core/logging.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/core/logging.py)（约 170-173 行）
- **修复**：循环中 `logger.setLevel(logging.INFO)`
- **验证**：启动后端，确认 "Uvicorn running on..." 日志出现

### I-16 `asyncio.create_task` 无异常回调

> ✅ **已修复**（见顶部「第五批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/api/v1/assets.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/assets.py)（约 104-129 行）
- **修复**：`task.add_done_callback(lambda t: t.exception() and logger.exception(...))`
- **验证**：mock 同步任务抛异常，断言日志记录

### I-17 `get_targets` 静默吞异常返回 `[]`

> ✅ **已修复**（见顶部「第五批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/prometheus/client.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/prometheus/client.py)（约 166-168 行）
- **修复**：让 `get_targets` 在网络/超时错误时抛出，由调用方决定降级；或返回 `tuple[list, bool]`（targets, is_success）
- **验证**：mock Prometheus 超时，断言 asset_sync 不误判

### I-18 `_extract_ip` 不校验 IP 格式

> ✅ **已修复**（见顶部「第五批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/services/prometheus/client.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/prometheus/client.py)（约 222-231 行）
- **修复**：用 `ipaddress.ip_address()` 校验，非法返回 `""` 并记 WARNING
- **验证**：单测覆盖 hostname/IP/异常输入

### I-19 证书列表移除分页，全量返回

> ✅ **已修复**（见顶部「第五批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：
  - [backend/app/api/v1/ops.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/ops.py)（约 245-262 行）
  - [frontend/src/pages/Ops/CertificateList.tsx](file:///d:/MYDATA/Include/MH_job/ops-manager/frontend/src/pages/Ops/CertificateList.tsx)
- **修复**：后端保留分页（上限 500），前端用 React Query `keepPreviousData` 优化
- **验证**：性能测试 1000 条证书

### I-20 迁移未过滤空字符串 `feishu_open_message_id=''`

> ✅ **已修复**（见顶部「第五批修复」小节，2026-07-17 验证通过；迁移创建时已包含过滤）。以下为原始方案保留备查。

- **文件**：[backend/alembic/versions/20260717_0001_create_alert_card_messages.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/alembic/versions/20260717_0001_create_alert_card_messages.py)（约 76-80 行）
- **修复**：`WHERE feishu_open_message_id IS NOT NULL AND feishu_open_message_id != ''`
- **验证**：构造空字符串记录，断言迁移后无脏数据

### I-21 `CertificateList.tsx` filter 变化时分页不重置

- **文件**：[frontend/src/pages/Ops/CertificateList.tsx](file:///d:/MYDATA/Include/MH_job/ops-manager/frontend/src/pages/Ops/CertificateList.tsx)（约 22-31 行）
- **修复**：filter 变化时 `setPagination(p => ({ ...p, current: 1 }))`
- **验证**：手动测试

### I-22 `/suggestions/track` 菜单与路由权限不一致

- **文件**：
  - [frontend/src/config/menuPermissionConfig.ts](file:///d:/MYDATA/Include/MH_job/ops-manager/frontend/src/config/menuPermissionConfig.ts)（约 69 行）
  - [frontend/src/config/routePermissions.ts](file:///d:/MYDATA/Include/MH_job/ops-manager/frontend/src/config/routePermissions.ts)（约 23 行）
- **修复**：`menuPermissionConfig.ts` 给该项补 `permission: 'suggestion:submit'`
- **验证**：手动测试

### I-23 `SuggestionListResponse` 分页元数据不完整

- **文件**：[backend/app/schemas/suggestion.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/schemas/suggestion.py)（约 70-74 行）
- **修复**：补充 `page`、`pageSize`、`totalPages` 字段
- **验证**：API 测试

### I-24 回调路径 `market_result` 无长度校验

> ✅ **已修复**（见顶部「第三批修复」小节，2026-07-17 验证通过）。以下为原始方案保留备查。

- **文件**：[backend/app/integrations/feishu/callback_handler.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py)（约 613-617, 920-925 行）
- **修复**：对 `market_result` 做 `[:2000]` 截断或长度校验，与 schema 一致
- **验证**：单测构造超长字符串

---

## P2 — Minor / 性能 / 可维护性（可选）

> 列表见审查报告原文。建议在 P0/P1 完成后逐项处理。重点项：
>
> - `CRUDBerminalMetric` 类名拼写
> - `feishu_open_message_id` deprecated 标记
> - `departments.py` users 双重加载
> - 主键冗余索引
> - `query_code`/`open_id` 日志脱敏（如归入安全类则跳过）
> - lark_md 内容转义
> - `submitter_id` `ondelete=SET NULL` 孤儿建议
> - `SuggestionTrack` queryKey 反模式
> - `optionFilterProp` 与 `filterOption` 冲突
> - `get_targets()` 重复调用
> - 迁移 `IF NOT EXISTS` 一致性
> - 模型与迁移索引一致性
> - `.coveragerc` 补 `omit`
> - `SuggestionList` 错误状态展示
> - Layout.tsx 菜单 permission 配置
> - `status` 查询参数 Enum 校验
> - `down_revision` 伪 hash 核对

---

## 修复顺序建议

1. **第一批（无依赖，可并行）**：C-13（临时文件）、C-12（健康检查阈值）、C-11（refresh relationship）、I-15（uvicorn level）、I-21（前端分页重置）、I-22（菜单权限）、I-23（分页元数据）
2. **第二批（告警通知核心）**：C-01、C-02、C-03、C-04、C-05、C-06、I-01、I-02、I-04、I-05、I-11
3. **第三批（建议功能）**：C-07、C-08、I-06、I-07、I-09、I-24
4. **第四批（CRUD/模型）**：C-09、C-10、I-12、I-13、I-14
5. **第五批（迁移/Prometheus）**：I-08、I-17、I-18、I-19、I-20、I-16
6. **第六批（P2 Minor）**：按需

---

## 验证总览

- **单元测试**：扩展 [backend/tests/services/](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/tests/services/) 下已有测试文件
- **并发测试**：使用 pytest-asyncio + asyncio.gather 模拟并发 webhook/审批
- **迁移测试**：每个迁移 upgrade/downgrade 各跑一次，验证幂等性
- **回归测试**：在 8000 端口直接测试接口（遵循项目约束，不重启后端）
- **前端构建**：每次前端改动后 `npm run build`（遵循项目约束）
- **闭环测试**：每项修复必须有对应的测试用例验证，作为完成的唯一判断标准

---

## 风险与回滚

- 每项修复独立提交，便于回滚
- 涉及迁移的修复（C-06、C-09、I-08、I-20）需先在测试环境演练
- C-04 部分唯一索引方案需评估对历史数据的影响
- C-09 数据清理需备份

---

# 第二轮对抗式审查验证结论（2026-07-17）

> 本节为修复计划执行后，再次开启对抗式审查的验证结论。验证方式：5 个并行子代理分别对告警通知、建议功能、CRUD/模型、迁移/Prometheus/前端、临时文件/git 状态进行代码级落地验证。

## 总体修复落地情况

| 状态 | 数量 | 占比 |
|------|------|------|
| ✅ 已正确修复 | 19 项 | 73% |
| ⚠️ 修复有缺陷 | 6 项 | 23% |
| ❌ 未修复 | 1 项 | 4% |

**已正确修复（19 项）**：C-01、C-03、C-05、C-06、C-07、C-08、C-09、C-10、C-11、C-12、C-13、I-02、I-04、I-05、I-08、I-11、I-15、I-16、I-17、I-18、I-19、I-20、I-21、I-22、I-23、I-09

**修复有缺陷（6 项）**：C-02、C-04、I-01、I-06、I-07、I-12、I-13、I-14、I-24

**未修复（1 项）**：I-03

## 🔴 P0 阻断合并的严重缺陷（新发现，3 项）

### NC-1 I-14 `create_with_labels` 引入必然失败的回归 Bug

- **文件**：[backend/app/crud/crud_asset.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_asset.py)（约 158-168 行）
- **问题**：合并 commit 后 `db_obj.id` 在 flush 前为 None，`AssetHistory.asset_id` NOT NULL 约束违反。任何创建资产的请求都会 500，资产与历史均不写入。
- **攻击/故障场景**：`POST /assets` 创建资产接口整体不可用。I-14 把"双 commit 非原子"变成了"单 commit 必失败"。
- **修复建议**：在 `db.add(history)` 前加 `await db.flush()` 让 `db_obj.id` 先生成；或为 `AssetHistory` 增加 `asset = relationship("Asset")` 并通过 `history.asset = db_obj` 关联让 SQLAlchemy 自动同步 FK。
- **回归风险**：高。影响资产创建主流程。若有最基础的创建资产闭环测试，此 bug 会立即暴露——这正是"无测试"付出的代价。

### NC-2 I-06 fallback 8-char 路径是死代码且会 500

- **文件**：[backend/app/services/suggestion_service.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/suggestion_service.py)（约 50 行）
- **问题**：`query_code` 列为 `VARCHAR(6)`（model 与 migration 均如此），但 fallback 生成 8 字符。触发时（6-char 路径 20 次全部冲突，极小概率）会抛 `DataError: value too long for type character varying(6)`。`DataError` 不是 `IntegrityError` 的子类，调用方 `except IntegrityError` 无法捕获，直接 500。
- **攻击/故障场景**：6-char code 池接近耗尽时（理论容量 36^6 ≈ 21 亿，实际受限于字母数字组合），fallback 路径触发即 500。
- **修复建议**：
  1. 将 model 和 migration 的 `String(6)` 改为 `String(8)`（需新增迁移），或
  2. fallback 也使用 6 字符（增加循环次数到 30+），或
  3. 调用方增加 `except DataError` 捕获。

### NC-3 C-02 遗漏分支导致告警可能误标记

- **文件**：[backend/app/services/alerts/notification_task.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/alerts/notification_task.py)（约 170 行）
- **问题**：「无飞书模板」路径调用 `_update_alert_history_notification_sent` 未传 `starts_at` 参数，走 `else` 退化路径（无 starts_at 过滤、无 status 过滤），可能误标记其他周期/其他告警的 firing 记录为 `notification_sent=True`。
- **攻击/故障场景**：管理员删除默认飞书模板后，所有 webhook 走此分支；同 instance 上多周期 firing 告警会互相误标记，造成告警永久丢失。
- **修复建议**：第 170 行调用补 `starts_at=starts_at` 参数。

## 🟠 P1 重要缺陷（新发现，7 项）

### ND-1 C-04 IntegrityError 重试 SELECT 未过滤 `status='firing'`

- **文件**：[backend/app/crud/crud_alert.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_alert.py)（约 189-201 行）
- **问题**：IntegrityError 捕获后重试 SELECT 时未加 `.where(AlertHistory.status == 'firing')` 过滤，会命中已 resolved 的记录并加锁；调用方 UPDATE 路径会把已 resolved 记录改回 firing，与 I-01 修复意图冲突。
- **修复建议**：重试 SELECT 加 `.where(AlertHistory.status == 'firing')` 过滤。
- **补充说明**：C-04 的 `with_for_update()` 在 SELECT 返回 0 行时不获取任何锁（PG 行锁只锁命中行），对"无 existing_record"场景无效，依赖部分唯一索引兜底。这是已知设计权衡，但说明 `with_for_update` 不是真正修复。

### ND-2 I-01 `starts_at=None` 时跳过批量 resolve

- **文件**：[backend/app/api/v1/alerts.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/alerts.py)（约 152-185 行）
- **问题**：当 `starts_at is None` 时整个批量 resolve 块被跳过，旧 firing 记录状态保持 firing。后续虽会创建新 resolved 记录，但 `prepare_alert_notification` 在 resolved 路径下查询待更新卡片时可能查到旧 firing 卡片。
- **修复建议**：`starts_at is None` 时 fallback 使用 `now_shanghai()`，或显式拒绝 webhook 并记录 WARNING。

### ND-3 I-07 `update_card_message` 路径无重试且吞异常

- **文件**：[backend/app/services/suggestion_service.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/services/suggestion_service.py)（约 325-329、415-419、435-439 行）
- **问题**：`update_to_approved_sync`/`update_to_rejected_sync`/`update_to_archived_sync` 直接调用 `update_card_message`，未使用 `_send_with_retry`。且 `update_card_message` 内部 catch 异常返回 `{"success": False}`，不抛异常，调用方的 `except Exception` 永远不会触发，返回值也未被检查。
- **故障场景**：审批通过后更新原卡片时网络瞬断 → 卡片仍显示"待审批" → 用户再次点击审批 → C-07 的 rowcount=0 跳过 → 卡片永久显示错误状态，无任何重试或告警。
- **修复建议**：1) `update_card_message` 也用 `_send_with_retry` 包装；2) 或检查返回值 `success` 字段，失败时记 ERROR 并重试。

### ND-4 I-13 owner_name 解析失败时数据不一致

- **文件**：[backend/app/crud/crud_asset.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_asset.py)（约 192-198 行）
- **问题**：用户传 `owner_name="不存在的名字"`（非 None）且未传 `owner_id` 时，`_resolve_owner_id_by_name` 返回 `(None, False)`，`owner_id` 不更新，但循环 `setattr(db_obj, "owner_name", "不存在的名字")` 仍执行。结果：`owner_name` 改了，`owner_id` 保留旧值（指向另一个用户），数据不一致。
- **修复建议**：owner_name 解析失败时，要么拒绝整个更新（raise），要么同步清空 `owner_name`（设为 None），不应保留不一致状态。

### ND-5 I-12 `has_duplicates` 被调用方丢弃

- **文件**：[backend/app/crud/crud_asset.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/crud/crud_asset.py)（约 130、193 行）
- **问题**：`_resolve_owner_id_by_name` 返回 `(owner_id, has_duplicates)` 元组，但调用方均为 `resolved_owner_id, _ = await _resolve_owner_id_by_name(...)`，重名标志被 `_` 丢弃。即便检测到重名，调用方既不拒绝操作、也不向上层返回警告，最终行为仍是"取 id 最大的一条"——与原 `limit(1)` 在功能层等价，只是多了日志。
- **次要问题**：新增 `feishu_open_id.isnot(None)` 过滤改变查询语义，原 `limit(1)` 代码无此过滤，可能漏掉无 feishu_open_id 的用户。
- **修复建议**：1) 调用方检查 `has_duplicates`，重名时 raise 或返回 409 让用户明确选择 user_id；2) 评估 `feishu_open_id.isnot(None)` 过滤是否必要，或改为可选参数。

### ND-6 I-24 API 路径 `archive_suggestion` 写入 market_result 无截断

- **文件**：[backend/app/api/v1/suggestions.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/api/v1/suggestions.py)（约 407 行）
- **问题**：回调路径 `_handle_suggestion_archive` 已截断到 2000 字符，但 API 路径 `archive_suggestion` 端点 `suggestion.market_result = data.market_result` 无任何长度校验。虽然 DB 列是 `Text`（无长度限制），但超长文本会导致：1) 飞书卡片渲染异常；2) 与回调路径的 2000 字符限制行为不一致。
- **修复建议**：在 `archive_suggestion` API 端点或 schema 层统一截断到 2000 字符，或提取公共截断函数。

### ND-7 I-03 `_try_forward_callback` 仍每次新建 engine，异常路径不 dispose

- **文件**：[backend/app/integrations/feishu/callback_handler.py](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py)（约 41-322 行）
- **问题**：`_try_forward_callback` 仍在 line 47 直接 `create_engine()`，每次新建 engine。异常路径（line 285 except block）不 dispose 原 engine——仅创建新 engine 记日志。新增的 `_get_sync_engine()` helper 不是模块级共享 engine，每次调用仍创建新 engine。其他函数（`_handle_suggestion_approve` 等）虽用 `_get_sync_engine()`，但 except 块同样不 dispose。
- **修复建议**：使用 `try/finally` 确保 dispose：
  ```python
  engine = create_engine(...)
  try:
      # 业务逻辑
  finally:
      engine.dispose()
  ```
  或使用模块级共享 engine（需注意线程安全）。

## 🟡 关键风险提示

### 风险 1：测试覆盖严重不足（违反工作区规则）

- `test_notification_task_unit.py` 与 `test_alert_failure_paths.py` 因 import 已删除函数**完全无法运行**
- `test_db_session_lifecycle.py` 全部 skip
- **C-06、C-07、C-09、I-07 等核心修复项零测试覆盖**
- 违反工作区规则"功能改造或者新增的唯一判断标准是闭环测试"
- **建议**：补充针对状态机并发、query_code 竞态、重试逻辑、ON CONFLICT、create_with_labels 的测试用例

### 风险 2：诊断脚本残留

- `backend/scripts/check_card_messages.py` 为新增未跟踪诊断脚本，未命中 `.gitignore` 的 `_tmp_*` 规则，存在误提交风险
- **建议**：用完即删，或重命名为 `_tmp_check_card_messages.py` 以命中现有忽略规则，或在 `.gitignore` 追加 `backend/scripts/check_card_messages.py`

### 风险 3：日志敏感信息

- [callback_handler.py:1929,1963,1997](file:///d:/MYDATA/Include/MH_job/ops-manager/backend/app/integrations/feishu/callback_handler.py) 三处 `lark.JSON.marshal(data)` 序列化整个飞书事件对象，间接记录用户 open_id（PII）
- **建议**：替换为只记录必要字段（如 `event_type`、`message_id`、`open_id[:8]...` 截断）

## ✅ 通过对抗式审查的修复亮点

- **三阶段分离架构得到严格遵守**：C-01 回滚位置正确在阶段 3 `save_notification_result` 内，未污染阶段 2 `execute_feishu_notification`，符合核心不变式
- **迁移链完整**：0001→0002→0003→0004 链式串联，upgrade/downgrade 齐全，无分叉断链
- **C-04 部分唯一索引设计正确**：`CREATE UNIQUE INDEX ... WHERE status='firing'` 不影响历史 resolved 数据，先清理重复再建索引
- **C-06 ON CONFLICT 覆盖所有运行时 INSERT 路径**：全局 grep 确认仅 `_save_firing_alert_card_messages` 一处运行时 INSERT
- **I-09 同步版本真同步**：无 `asyncio.run` 残留（仅调度器启动脚本有，不在范围内），async 版本用 `asyncio.to_thread` 薄包装
- **C-13 临时文件彻底清理**：索引、磁盘、.gitignore 三处都到位
- **C-07 状态机修复全覆盖**：approve/reject/archive 三处 UPDATE 均加 `WHERE status=...` + `rowcount` 检查 + rowcount=0 跳过后续卡片操作

## 修复优先级建议（第二轮）

**立即修复（P0，阻断合并）**：
1. NC-1（I-14 create_with_labels 必失败）— 资产创建接口不可用
2. NC-2（I-06 fallback DataError）— 极端场景 500
3. NC-3（C-02 遗漏分支）— 告警永久丢失

**合并前修复（P1）**：ND-1 ~ ND-7

**后续迭代（P2）**：
- 补充测试覆盖（C-06、C-07、C-09、I-07、I-14 等）
- 清理诊断脚本 `check_card_messages.py`
- 日志脱敏 `lark.JSON.marshal(data)` 三处
- 清理 dead import `contextlib`（alerts.py:5）
- 评估 C-10 `bulk_upsert_metrics` 死代码（全项目无调用方）

